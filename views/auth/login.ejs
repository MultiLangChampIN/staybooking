<!DOCTYPE html>
<html lang="en">
<%- include("../partials/head") %>

    <body>
        <%- include("../partials/nav") %>

            <div class="max-w-sm mx-auto mt-12 border border-gray-200 rounded-lg shadow-md overflow-hidden">
                <div class="flex border-b border-gray-200">
                    <button
                        class="tab-btn active flex-1 py-3 px-4 text-center font-medium text-gray-700 bg-gray-50 hover:bg-gray-100 border-b-2 border-red-500 transition" onclick="showTab('login')"
                        >Login</button>
                    <button
                        class="tab-btn flex-1 py-3 px-4 text-center font-medium text-gray-700 bg-gray-50 hover:bg-gray-100 transition"
                        onclick="showTab('signup')">Sign Up</button>
                        
                </div>

                <div id="login" class="form-content active p-6">
                    <h2 class="text-2xl font-bold text-gray-800 mb-4">Login</h2>
                    <form action="/login" method="POST"  class="space-y-4">
                        <%- include("../partials/error") %>
                        <input type="email" name="email" placeholder="Email" value="<%=oldInput.email ? oldInput.email:''%>"
                            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-red-500"
                            required>
                        <input type="password" name="password" placeholder="Password"
                            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-red-500"
                            required>
                        <button type="submit"
                            class="w-full py-2 bg-red-500 text-white font-medium rounded-lg hover:bg-red-600 transition">Login</button>
                    </form>
                </div>

                

            </div>

            <script>
                function showTab(tabName) {
                    // Hide all form content
                    const contents = document.querySelectorAll('.form-content');
                    contents.forEach(content => content.classList.add('hidden','hidden'));
                    
                    // Remove active class from all buttons
                    const buttons = document.querySelectorAll('.tab-btn');
                    buttons.forEach(button => button.classList.remove('active', 'border-b-2', 'border-red-500'));
                    
                    // Show selected tab
                    if(tabName==='signup'){
                        window.location.href='/signup';
                    }
                    // document.getElementById(tabName).classList.remove('hidden','hindden')
                    
                    // Add active class to clicked button
                    event.target.classList.add('active', 'border-b-2', 'border-red-500');
                }
                
                
            </script>

    </body>

</html>
